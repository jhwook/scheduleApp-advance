package com.sparta.scheduleappadvance.repository;

import com.sparta.scheduleappadvance.entity.ScheduleAddedUser;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScheduleAddedUserRepository extends JpaRepository<ScheduleAddedUser, Long> {
}
